import React from "react";

const base_path = "/next_app_layout";

const url_adresy = {
    home: `${base_path}/`,
    about: `${base_path}/about`,
    services: `${base_path}/services`,
    contacts: `${base_path}/contacts`
}

export default url_adresy